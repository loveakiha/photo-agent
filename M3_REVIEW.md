这几个问题其实是 M3 最核心的一层。结合你现在的实际环境——**本机已有 llama.cpp + Qwen3.8-27B，但 4090D 显存已经基本吃满**——我建议不要为了 M3 再折腾一套 Ollama。

我的建议如下。

### Q-A：LLM 后端

**先用现有的 llama.cpp + Qwen3.8-27B。**

原因很简单：

* 已经部署完成；
* 已经有 OpenAI-compatible `/v1` 接口；
* Qwen3.8-27B 本身具备比较强的工具调用/指令执行能力；
* M3 当前重点是验证 **Agent 架构是否成立**，不是重新折腾推理后端。

但有一个现实问题：

> **M3 Agent 和现有照片分析任务不能无脑同时把显存塞满。**

你现在 27B + mmproj 已经接近：

```text
24.0G / 24.5G
```

所以我建议 M3 第一阶段采用：

**同一个 llama-server，Agent 和其他任务复用模型；暂时不再启动第二个 LLM。**

如果后面发现 Agent tool calling 和 M2 的 VLM 分析抢显存/性能，再考虑：

* 调低上下文；
* 调整模型量化；
* Agent 使用更小的专用模型；
* 或者把 Agent 与 VLM 分成两个推理服务。

**现在都不要提前做。**

---

# Q13：Agent 入口

我建议：

> **第一版：CLI + 现有 Web UI/HTTP 接口，暂时不专门开发新的 Web Agent UI。**

也就是说先做：

```text
photo-agent ask "帮我找出去年旅行中最好的照片"
```

或者：

```text
photo-agent agent
```

进入交互模式：

```text
You:
找出去年旅行中的重复照片

Agent:
我先搜索符合时间范围的照片……

Tool:
search_photos(...)

Agent:
找到 326 张，我现在检查其中的重复组……

Tool:
get_duplicate_groups(...)

Agent:
发现 42 个重复组……
```

这样最大的好处是：

> **先验证 Agent 的“大脑和工具链”，不要同时开发 UI。**

等 Agent 能稳定完成任务之后，再把这个入口放到 Web UI 里，只是换一个前端。

---

# Q14：7 个只读工具

**同意，而且我建议严格按照这个方案执行。**

第一阶段：

```text
search_photos
get_photo
get_metadata
get_quality
get_duplicate_groups
get_similar_photos
generate_report
```

全部是：

> **只读。**

这非常重要。

因为 M3 最开始我们要验证的是：

> **LLM 能不能正确理解用户 → 选择工具 → 获得信息 → 继续推理 → 得出结果。**

而不是马上测试：

> “AI 会不会把我的照片删了。”

所以第一阶段甚至可以把 M3 定义成：

> **Read-only Agent。**

---

## 这 7 个工具其实可以分成三层

### 第一层：找

```text
search_photos
```

回答：

> “有哪些照片符合我的条件？”

---

### 第二层：看

```text
get_photo
get_metadata
get_quality
```

回答：

> “这张照片具体是什么情况？”

---

### 第三层：关系分析

```text
get_duplicate_groups
get_similar_photos
```

回答：

> “这些照片之间是什么关系？”

最后：

```text
generate_report
```

负责：

> “把分析结果整理成用户可以看的报告。”

这套工具已经足够让 Agent 做出相当复杂的事情了。

---

# Q16：允许自主连续 Tool Calling？

**明确选择：允许。**

而且我认为：

> **这是 M3 必须实现的核心能力。**

否则 M3 其实只是：

```text
用户
 ↓
LLM
 ↓
调用一个工具
 ↓
返回
```

这还不算真正有意思的 Agent。

---

比如用户：

> “找出去年旅行中最值得保留的照片。”

Agent 可以：

```text
① search_photos
       ↓
② 得到 500 张
       ↓
③ get_quality
       ↓
④ 找出高质量照片
       ↓
⑤ get_similar_photos
       ↓
⑥ 去掉大量重复构图
       ↓
⑦ 得到候选
       ↓
⑧ 返回结果
```

这才是我们想验证的东西。

---

## 但是“自主循环”必须有护栏

这里我建议你注意一个很重要的区别：

> **允许模型自主决定下一步 ≠ 允许模型无限制调用工具。**

必须设置：

```text
最大 Tool Call 次数
最大执行时间
最大返回数据量
```

例如第一版可以：

```text
单次任务最多 20 次 tool calls
```

超过：

```text
Agent：
“当前任务涉及的检索步骤较多，我需要缩小范围后继续。”
```

否则一个模型如果陷入：

```text
search
→ search
→ search
→ search
→ search
```

就可能无限烧 token。

这和你之前研究的**多轮任务接力/有限 token 工作流**其实是同一个问题。

---

# Q17：文件修改做到哪一级？

这里我建议：

> **M3 第一阶段只做到一级：只读。**

也就是说：

### M3.0

```text
搜索
查看
分析
分组
评分
报告
```

全部可以直接执行。

但是：

```text
move
rename
delete
```

**暂时全部不存在。**

---

# 为什么我不建议 M3 一开始就做 move？

因为这是一个非常好的工程切分点。

我们先验证：

```text
用户意图
 ↓
Agent
 ↓
工具
 ↓
数据库
 ↓
结果
```

这一整条链。

如果发现 Agent：

* 理解时间范围有问题；
* 工具参数经常错误；
* 会重复搜索；
* 不知道什么时候该继续；
* 返回结果太多；
* 上下文容易失控；

我们可以在**没有破坏用户文件的情况下**把这些问题全部修掉。

---

然后进入 M3.1：

```text
move
rename
```

这时加入：

```text
Plan
 ↓
Dry-run
 ↓
用户确认
 ↓
执行
```

最后才考虑：

```text
delete
```

而且我甚至倾向于把：

> **永久删除**

放到 M4，而不是 M3。

---

# 所以这轮我建议最终锁定为

| 问题                   | 决策                                        |
| -------------------- | ----------------------------------------- |
| **Q-A LLM**          | **现有 llama.cpp + Qwen3.8-27B**            |
| **Q13 Agent 入口**     | **先 CLI / HTTP，暂不开发专用 Web Agent UI**      |
| **Q14 Tool**         | **7 个只读工具，完全同意分批**                        |
| **Q16 Tool Calling** | **允许自主连续调用**                              |
| **Q17 文件操作**         | **M3.0 只读；M3.1 再做 move/rename；delete 后置** |

---

## 我认为 M3.0 的真正目标应该重新定义成一句话

> **让 Qwen3.8-27B 能够通过一组安全的只读 Tool，自主完成“搜索 → 查询 → 分析 → 综合 → 回答”的多步骤照片任务。**

只要这个跑通，M3 就已经跨过了一个非常重要的坎：

```text
M0
数据库

M1
算法

M2
照片理解

       ↓

M3
       AI 开始“使用”前面所有能力
```

而不是再给它堆一个“聊天框”。

**下一轮我建议继续确认 Q15、Q18、Q19、Q20：自然语言搜索边界、任务上下文、Agent 出错/不确定时怎么办，以及 M3 的最终验收任务。** 这四个定下来，M3 的架构基本就完整了。