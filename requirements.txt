typer>=0.12
PyYAML>=6.0
Pillow>=10.0
pytest>=8.0

# M1 dependencies
imagehash>=4.3

# HEIC/HEIF support (iPhone originals); required when config lists .heic
pillow-heif>=0.16

# M2 dependencies (technical quality measurement, CPU-only)
opencv-python>=4.9
numpy>=1.26

# Later milestones
# torch / clip (M3a embeddings)
# ollama client (M3b VLM)
